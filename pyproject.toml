[project]
name = "motion-amplifier"
version = "1.0.0"
description = "Small script to try and show motion in a video during playback"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opencv-python>=4.13.0.92",
]
